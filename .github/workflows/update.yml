# ─────────────────────────────────────────────────────────────────────────
# DEPRECATED — superseded by .github/workflows/update-daily.yml
#
# This file used to define the scheduled update workflow. It has been
# disabled (no `on:` triggers, no jobs) so it will NEVER run. Kept in
# the repo only so existing references don't 404; safe to delete via:
#
#   git rm .github/workflows/update.yml
#   git commit -m "chore: remove deprecated update.yml workflow"
#
# All scheduled work is now in update-daily.yml.
# ─────────────────────────────────────────────────────────────────────────
name: HR Tracker — DEPRECATED (see update-daily.yml)

on: {}     # no triggers — workflow will not run
jobs: {}   # no jobs — workflow has nothing to do
